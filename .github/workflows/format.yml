name: Format check

on:
  pull_request:
  push:
    branches:
      - main
      - staging
      - trying

jobs:
  check:
    name: Format check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Check Formatting
        run: cargo fmt -- --check
